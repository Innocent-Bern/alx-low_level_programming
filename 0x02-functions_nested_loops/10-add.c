#include "main.h"
/**
* add - Entry point
* @j: one of the intergers
* @n: one of the intergers
* Description: adds two integers and returns the result
* Return: int
*/
int add(int j, int n)
{
	return (j + n);
}
